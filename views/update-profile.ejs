<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Profile</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            text-align: center;
        }

        form {
            width: 40%;
            margin: 20px 0;
            text-align: center;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        label {
            display: inline-block;
            margin: 10px 0;
            font-weight: bold;
            width: 150px; /* Adjust the width based on your preference */
        }

        input {
            width: calc(100% - 160px); /* Adjust the width based on your preference */
            padding: 8px;
            margin-bottom: 10px;
            box-sizing: border-box;
        }

        button {
            padding: 10px;
            cursor: pointer;
            background-color: #4caf50;
            color: #fff;
            border: none;
            border-radius: 4px;
        }

        button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <h1>Update Profile</h1>

    <form action="/update-profile/<%= user._id %>" method="post" enctype="multipart/form-data">
        <label for="name">Name:</label>
        <input type="text" name="name" id="name" value="<%= user.name %>" required>

        <label for="email">Email:</label>
        <input type="text" name="email" id="email" value="<%= user.email %>" readonly>

        <label for="profession">Profession:</label>
        <input type="text" name="profession" id="profession" value="<%= user.profession || '' %>">

        <label for="hobby">Hobby:</label>
        <input type="text" name="hobby" id="hobby" value="<%= user.hobby || '' %>">

        <label for="profileImage">Profile Image:</label>
        <input type="file" name="profileImage" id="profileImage">

        <button type="submit">Update Profile</button>
    </form>
</body>
</html>
